server:
  port: 8090
spring:
  application:
    name: consul-config-server
#端点开放
management:
  endpoints:
    web:
      exposure:
        #暴露所有端点 默认是info,health
        include: "*"
#my:
#  prop: this default




